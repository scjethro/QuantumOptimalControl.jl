module solve

function solve(problem;kwargs...)
    # then we need to solve the problem
    # in future need to check which algorithm has been specified
    # __solve(problem, alg)
    
    dCRAB(problem)
    # alg = dCRAB
    # alg(problem)

end

end